__pycache__/
*.pyc
.coverage
coverage.xml
.hypothesis/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/

# CODESYS per-user and per-machine leftovers. cell.project itself IS committed:
# it carries the Modbus channel configuration, which build_project.py cannot
# reproduce, so the binary is the only place that configuration exists.
*.opt
*.precompilecache
plc/codesys/build.log
# Download artifacts, regenerated on every build, and CODESYS backup files.
*.bootinfo
*.bootinfo_guids
*.compileinfo
*.~u
plc/codesys/sync.log
plc/codesys/demo.log
certs/

# Screen recordings. The demo mp4 is a release asset, not a git object: it is
# megabytes of binary that changes wholesale every time it is re-recorded.
# scripts/record_demo.py reproduces it.
recordings/
